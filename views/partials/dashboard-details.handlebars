<section class="card text-center mt-3 mb-3">
  <header class="font-weight-bold" id="get-post-num" postnum="{{id}}"><div
      class="float-left"
    >{{title}}</div>
    <div class="float-right" style="font-size: 10px">posted on:
      {{postdate}}
      by you</div></header>
  <div>{{body}}</div>
  <div class="text-right">
    <br />
    <i
      id="trash"
      class="fa-solid fa-trash-can"
      style="margin-right: 15px; margin-bottom: 15px"
    ></i>
  </div>

</section>